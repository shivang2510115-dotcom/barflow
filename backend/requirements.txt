fastapi==0.110.1
uvicorn==0.25.0
boto3>=1.34.129
requests-oauthlib>=2.0.0
cryptography>=42.0.8
python-dotenv>=1.0.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
passlib>=1.7.4
tzdata>=2024.2
motor==3.3.1

# Firestore, which is what deploys (DB_BACKEND=firestore). The async client, not the
# sync one on a thread pool: this app is FastAPI and every call site already awaits.
# Kept below 3.0 because that is a major version nothing here has been run against.
google-cloud-firestore>=2.16,<3
pytest>=8.0.0
pytest-xdist>=3.6.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
python-jose>=3.3.0
requests>=2.31.0
pandas>=2.2.0
numpy>=1.26.0
python-multipart>=0.0.9
# The Excel half of the stock import. Chosen because it is small — one pure-Python
# package plus et-xmlfile — and this deploys as a Firebase Function, where every
# megabyte in the image is cold-start latency paid by requests that are not uploading a
# spreadsheet. pandas would read the same file and is already above for other reasons,
# but pulling it into this path would have been the wrong trade for one sheet of stock.
# Imported lazily inside services/inventory_import.py, so if it is ever absent the import
# degrades to "export a CSV" rather than taking the inventory router down at startup.
openpyxl>=3.1,<4
jq>=1.6.0
typer>=0.9.0
